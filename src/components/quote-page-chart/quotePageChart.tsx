import React from 'react';
import {   REQUEST_STATUSES, IStock } from '../../types'
const QuotePageChart: React.FC<IStock> = ({Stock}) => {
    return ( <div>
<div>Chart</div>
    </div> );
}
 
export default QuotePageChart;